<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= foods.name %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= foods.name %></h1>


    <a href="/users/<%= user._id %>/foods/<%= foods._id %>/edit">Edit this food</a>

    <form
      action="/users/<%= user._id %>/foods/<%= foods._id %>?_method=DELETE"
      method="post">
      <button type="submit">Delete this food</button>
    </form>
  </body>
</html>
